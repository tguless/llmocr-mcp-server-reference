<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="00015-create-users-table" author="system">
        <comment>Create users table for authentication</comment>

        <createTable tableName="users" schemaName="mcp_invoice">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="username" type="VARCHAR(100)">
                <constraints nullable="false" unique="true"/>
            </column>

            <column name="email" type="VARCHAR(255)">
                <constraints nullable="false" unique="true"/>
            </column>

            <column name="password_hash" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>

            <column name="first_name" type="VARCHAR(100)"/>
            <column name="last_name" type="VARCHAR(100)"/>

            <column name="tenant_id" type="VARCHAR(100)">
                <constraints nullable="false"/>
            </column>

            <column name="role" type="VARCHAR(50)" defaultValue="USER">
                <constraints nullable="false"/>
            </column>

            <column name="is_active" type="BOOLEAN" defaultValueBoolean="true">
                <constraints nullable="false"/>
            </column>

            <column name="is_email_verified" type="BOOLEAN" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>

            <column name="last_login_at" type="TIMESTAMP"/>

            <column name="created_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="updated_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="created_by" type="VARCHAR(100)"/>
        </createTable>

        <!-- Indexes -->
        <createIndex indexName="idx_users_tenant_id" tableName="users" schemaName="mcp_invoice">
            <column name="tenant_id"/>
        </createIndex>

        <createIndex indexName="idx_users_email" tableName="users" schemaName="mcp_invoice">
            <column name="email"/>
        </createIndex>

        <createIndex indexName="idx_users_username" tableName="users" schemaName="mcp_invoice">
            <column name="username"/>
        </createIndex>

        <!-- Create refresh tokens table -->
        <createTable tableName="refresh_tokens" schemaName="mcp_invoice">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="user_id" type="BIGINT">
                <constraints nullable="false" foreignKeyName="fk_refresh_token_user"
                           references="mcp_invoice.users(id)" deleteCascade="true"/>
            </column>

            <column name="token" type="VARCHAR(500)">
                <constraints nullable="false" unique="true"/>
            </column>

            <column name="expires_at" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="created_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="revoked" type="BOOLEAN" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <createIndex indexName="idx_refresh_tokens_user_id" tableName="refresh_tokens" schemaName="mcp_invoice">
            <column name="user_id"/>
        </createIndex>

        <createIndex indexName="idx_refresh_tokens_token" tableName="refresh_tokens" schemaName="mcp_invoice">
            <column name="token"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>

